# YAML-file validation

The entire `pem` workflow requires a significant amount of parameters and constants to run. They are all gathered in
a `YAML` file, which is validated through a `pydantic` process. The first time a YAML file is set up for a project,
it is advised to do it trough the interface
